# 阶段 1 / R-01 checkpoint

日期：2026-08-09
范围：修复文章、菜谱、公开笔记详情对全站日浏览趋势的误计数。

## 已完成

- 新增统一的 `ViewCounter` 编排器。
- 三个详情控制器仅在资源浏览量原子更新返回 `1` 时累加 `view_daily`。
- 无效 slug/id、未发布资源和限流窗口内重复访问不会污染全站趋势。
- 增加 `ViewCounterTest`，覆盖成功更新和零行更新两条分支。
- 增加集成回归：缺失资源、未发布文章、同 IP 去重和多客户端并发访问。

## 验证结果

- `mvn -q test`：71 个测试类、720 个测试通过；失败 0、错误 0、跳过 0。
- `git diff --check`：通过。

## 未处理范围

本 checkpoint 不包含 R-02/R-03/R-04 或阶段 1 的其他任务。前端依赖、Flyway 演示数据和菜谱异步化按后续独立批次执行。
