# 项目计划执行台账（2026-08-09）

> 对应路线图：`docs/project-audit-and-execution-plan-2026-08-09.md`
> 当前结论：阶段 0—6 的仓库实现与本地验收已完成。隔离恢复演练已通过；未执行未经授权的生产部署、数据库写入或 nginx reload。评论系统遵循路线图中的安全建议暂缓，不属于本次交付缺口。

## 阶段状态

| 阶段 | 状态 | 已完成 | 未完成或阻塞 |
| --- | --- | --- | --- |
| 0 基线 | 完成 | 基线、部署路线、单实例约束、备份、Flyway 版本、忽略规则 | 无 |
| 1 正确性与安全 | 完成 | 浏览计数、依赖升级、CSP Report-Only、生产配置校验、`Retry-After`、外部 URL 校验复核 | DNS pinning 和主机级出口策略属于后续基础设施加固 |
| 2 数据治理 | 完成 | 独立开发 seed、迁移前置检查、空库与恢复库迁移、附件回收站/清理/配额、V50 恢复演练、历史迁移不改写 | 无 |
| 3 异步可靠性 | 完成 | 菜谱提取 202 异步 Job、有界线程池、取消/重试/3 分钟超时、`yt-dlp` 网络/重试/文件边界、AI 原子预算、非流式重试与熔断 | 操作系统级 CPU/网络沙箱属于部署基础设施职责 |
| 4 架构与门禁 | 完成 | admin client/recipe API 拆分、OpenAPI TS 生成、ESLint、Prettier、Spotless、coverage、Playwright、axe、构建与审计 CI | 后续可继续按领域渐进拆分，但已建立门禁与边界 |
| 5 性能与部署 | 完成（仓库/只读生产验证） | 构建预算、PWA 预缓存瘦身、ETag、恢复库查询审计、nginx 强制/报告 CSP、唯一 nginx 路线、生产只读冒烟 9/9、移动 LCP 1.6 秒 | 服务器发布/reload/回滚未获授权，因此未执行外部状态变更 |
| 6 产品功能 | 完成 | 定时发布/取消、批量发布/草稿/归档/标签、审计；独立搜索中心；AI 动作/会话/可靠性/用量；周菜单/购物清单；图谱筛选/子图/JSON/SVG 导出；离线阅读缓存；既有角色协作 | 评论系统按路线图要求等待审核、反垃圾、举报、封禁与隐私制度后再立项 |

## 已执行的数据库动作

- 本地开发库备份成功：`outputs/db-backup/yubai_blog-local-20260809-014835.dump`，119254 字节。
- 迁移前置检查成功：本地 Flyway 当前版本 46，文章 15 条，菜品 20 条。
- 空库迁移测试成功；当前迁移版本为 V50。
- 隔离恢复演练成功：使用本机独立 PostgreSQL cluster 恢复备份并迁移 V46→V50；文章 15→15、菜品 20→20，V48/V49/V50 对象存在，演练进程已停止。
- 恢复库 `EXPLAIN ANALYZE` / `pg_stat_statements` 审计完成；小数据集使用顺序扫描合理，查询约 0.06 ms。
- 生产只执行 HTTPS 只读冒烟（9/9）；未执行生产备份、迁移、nginx reload、发布或回滚。

历史 V34/V39 仍包含演示数据。它们已经属于不可改写的迁移历史，因此“新空库不产生演示数据”不能通过修改历史迁移实现；后续演示数据入口已迁到显式的本地开发 seed 流程。

## 最终本地验证

- 后端：75 个 Surefire 报告，732 个测试，0 failure，0 error，0 skipped。
- 后端 JaCoCo：instruction 77.34%，branch 57.97%。
- 前端：65 个测试文件、805 个测试通过。
- 前端覆盖率：statements 66.74%，branches 61.25%，functions 57.85%，lines 69.07%（四项均高于原基线）。
- 前端类型检查、ESLint、Prettier、生产构建通过。
- Playwright + axe：3 个关键路径测试通过；移动首页 LCP 1.6 秒；修复 WCAG AA 对比度和全局搜索 Esc 键盘关闭问题。
- 依赖审计：生产依赖和全部依赖均为 0 个已知漏洞。
- 构建预算：43 个 JS 文件，总计 1,409,213 字节；按需代码高亮 chunk 815.46 KiB；PWA precache 839.01 KiB；首页不预加载/预缓存编辑器、KaTeX 与代码高亮大包。
- `git diff --check` 通过。

## 外部操作边界

仓库范围工作已完成。若要把 nginx 配置、V50 迁移和新前端产物发布到生产，还需要服务器连接与明确发布授权；生产数据库迁移、reload 和回滚均属于外部状态变更，不能由本地执行计划自动推定授权。
